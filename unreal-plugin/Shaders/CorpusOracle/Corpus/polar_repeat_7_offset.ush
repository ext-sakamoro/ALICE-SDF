// GENERATED by examples/unreal_corpus_oracle.rs from tests/common/corpus.rs — do not edit
// corpus node `polar_repeat_7_offset`: HlslShader::transpile(.., Hardcoded)
float alice_atan2(float y, float x) {
    bool sy = asint(y) < 0;
    bool sx = asint(x) < 0;
    float r = atan2(y, x);
    r = (x == 0.0) ? (sy ? -1.5707964 : 1.5707964) : r;
    r = (y == 0.0) ? (sx ? (sy ? -3.1415927 : 3.1415927) : y) : r;
    return r;
}

float sdf_eval(float3 p) {
    float d1 = alice_atan2(p.z, p.x);
    float d2 = 6.2831855 / 7.0;
    float d3 = 7.0 / 6.2831855;
    float d4 = floor(d1 * d3 + 0.5) * d2;
    float d5 = cos(d4);
    float d6 = sin(d4);
    float3 d7 = float3(d5 * p.x + d6 * p.z, p.y, -d6 * p.x + d5 * p.z);
    float3 d8 = d7 - float3(-3.0, 0.0, 3.0);
    float3 d9 = d8 - float3(0.0, -3.0, 0.0);
    float3 d10 = float3(0.0, 3.0, 0.0) - float3(0.0, -3.0, 0.0);
    float d11 = clamp(dot(d9, d10) / max(dot(d10, d10), 1e-10), 0.0, 1.0);
    float d12 = length(d9 - d10 * d11) - 3.0;
    float d0 = d12;
    return d0;
}
