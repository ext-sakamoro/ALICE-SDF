// GENERATED by examples/unreal_corpus_oracle.rs from tests/common/corpus.rs — do not edit
// corpus node `cone`: HlslShader::transpile(.., Hardcoded)
float sdf_eval(float3 p) {
    float d0 = length(p.xz);
    float d1 = 0.4;
    float2 d2 = float2(d0 - min(d0, ((p.y < 0.0) ? 0.5 : 0.0)), abs(p.y) - d1);
    float d3 = clamp((-d0 * -0.5 + (d1 - p.y) * 0.8) / 0.89000005, 0.0, 1.0);
    float2 d4 = float2(d0 + -0.5 * d3, p.y - d1 + 0.8 * d3);
    float d5 = ((d4.x < 0.0 && d2.y < 0.0) ? -1.0 : 1.0);
    float d6 = min(dot(d2, d2), dot(d4, d4));
    float d7 = d5 * sqrt(d6);
    return d7;
}
