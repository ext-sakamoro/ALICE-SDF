Place alice_sdf.dll and alice_sdf.lib here.

Build from source:
  cargo build --release --features "ffi hlsl glsl gpu" --target x86_64-pc-windows-msvc

Or download pre-built binaries from GitHub Releases.
